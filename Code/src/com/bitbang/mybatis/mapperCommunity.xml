<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="community">
	<select id="communityPostList">
		SELECT * FROM (SELECT ROWNUM R_NUM, B.* FROM ((SELECT *FROM COMMUNITY ORDER BY C_REPARENT DESC, C_REDEPTH ASC)) B) WHERE R_NUM BETWEEN #{START} AND #{END};
	</select>
</mapper>















